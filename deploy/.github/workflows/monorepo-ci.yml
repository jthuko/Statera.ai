name: monorepo-ci
on: [workflow_dispatch]
jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: isbang/compose-action@v2
        with: { compose-file: deploy/docker-compose.yml, up-flags: --build -d }
